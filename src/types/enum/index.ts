export enum UserRole {
    ADMIN = "ADMIN",
    MANAGER = "MANAGER",
    TEACHER = "TEACHER",
    STUDENT = "STUDENT",
}

export enum CookiesEnum {
    ACCESS_TOKEN = "accessToken",
    REFRESH_TOKEN = "refreshToken",
    USER = "user",
}
